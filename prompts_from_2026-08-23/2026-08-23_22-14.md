The paper of Hossein Naderi handles a real UniTree Quadruped Robot with real RGB-D and LiDAR cameras.

I do not have access to a real robot nor to laboratory. I also do not have a cluster of GPUs for DeepSeek and so on.

My idea is to have either a dataset of construction site images in the format like in the paper Naderi2026, or to have a simulation for quadruped robot, e.g. rViz, Gazebo etc.
I need to use Cloude APIs of Claude so that I do not have to buy and setup a cluster of local GPUs.

- Separate UniTree robot navigation and experiment with SLAM and Automatic Navigation
- After SLAM and navigation experiments are complete, get a construction site dataset for VLM
- Use a VLM API (if the model is too big) to extract the image description from the dataset
- Run the experiment on Cloud API LLM, achieving expected results